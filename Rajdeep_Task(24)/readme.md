# Task 24 - Tailwind CSS Animations & Interactive UI

---

## Technologies Used
- HTML5
- Tailwind CSS

---

## Project Structure

```text
Rajdeep_Task(24)/
│
├── assets/             
├── node_modules/       
├── index.html          
├── input.css           
├── output.css          
├── package.json       
├── package-lock.json   
└── README.md           
```

---

##  How to open

### 1. Download the ZIP file

### 2. Open the project folder

### 3. Install dependencies

```bash
npm install tailwindcss @tailwindcss/cli  
```

### 4. Generate Tailwind CSS

```bash
npx @tailwindcss/cli -i input.css -o output.css --watch
```

### 5. Open the project

Open `index.html` in your browser.

Or, if you're using VS Code:

1. Install the **Live Server** extension.
2. Right-click on `index.html`.
3. Click **Open with Live Server**.

## Author

**Rajdeep Das**


## Remarks

Sir, I would like to clarify that the code which I submitted before was typed by me. I only used AI to generate the notification badge icons because I couldn't find suitable icons. Those images were used only as assets and were not related to the code.

If there is any certain part of code that looks AI-generated or violates the task guidelines, I would be grateful if you could point it out. I really want to understand my mistake and improve. Thank you.